<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorPrimaryDark">

    <!-- Header: shows active channel name (we set this in Java) -->
    <LinearLayout
        android:id="@+id/active_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="24dp"
        android:paddingTop="24dp"
        android:paddingRight="24dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/active_channel_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textColor="?android:attr/textColorPrimaryInverse"
            android:textStyle="bold"
            android:text="@string/app_name"/>

        <TextView
            android:id="@+id/active_channel_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?android:attr/textColorSecondaryInverse"
            android:text="@string/touch_and_hold_to_talk"/>
    </LinearLayout>

    <!-- Center: big PTT -->
    <FrameLayout
        android:id="@+id/ptt_center"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Accent ring -->
        <View
            android:id="@+id/ptt_ring"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:layout_gravity="center"
            android:background="@drawable/ptt_ring_bg"/>   <!-- was @drawable/ptt_ring_shape -->


        <!-- PTT container (FrameLayout to avoid cast crash) -->
        <FrameLayout
            android:id="@+id/pushtotalk_view"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:layout_gravity="center"
            android:foregroundGravity="center"
            android:background="@drawable/ptt_button_bg">   <!-- was ptt_button_shape -->

            <Button
                android:id="@+id/pushtotalk"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:foreground="@null"
            android:stateListAnimator="@null"
            android:elevation="0dp"
            android:text=""
            android:contentDescription="@string/push_to_talk"/>

        </FrameLayout>
    </FrameLayout>

    <!-- Bottom spacer -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:gravity="center"
        android:orientation="horizontal" />

    <!-- ===== Hidden legacy containers kept for compatibility ===== -->

    <!-- Legacy pager kept but hidden; tab strip MUST be a direct child of the ViewPager -->
    <androidx.viewpager.widget.ViewPager
        android:id="@+id/channel_view_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0"
        android:visibility="gone">

        <androidx.viewpager.widget.PagerTabStrip
            android:id="@+id/channel_tab_strip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:textColor="?android:attr/textColorPrimaryInverse"
            android:background="?attr/colorPrimary" />
    </androidx.viewpager.widget.ViewPager>

    <!-- Legacy two-pane container kept but hidden -->
    <LinearLayout
        android:id="@+id/two_pane_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0"
        android:orientation="horizontal"
        android:visibility="gone">

        <FrameLayout
            android:id="@+id/list_fragment"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <FrameLayout
            android:id="@+id/chat_fragment"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />
    </LinearLayout>


    <LinearLayout
        android:id="@+id/target_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/target_panel_warning"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="?android:attr/textColorPrimaryInverse"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/target_panel_cancel"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="@string/cancelled" />
    </LinearLayout>

</LinearLayout>
